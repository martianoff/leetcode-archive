object Leet2158a extends App {

  import scala.util.chaining._

  object Solution {
    def amountPainted(paint: Array[Array[Int]]): Array[Int] = {
      // check intervals fully overlapping with new interval
      // if yes return 0
      // else
      // get all intervals with start/end points within new interval
      // find gaps
      // insert a new interval
      Array(0)
    }
  }

}
